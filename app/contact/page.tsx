import SimplePage from "../components/SimplePage";

export default function ContactPage() {
  return (
    <SimplePage title="Contact VisaPic" description="Contact information for the VisaPic project.">
      <p>
        Add your production support email, business address or contact form
        here before publishing the site.
      </p>
      <p>
        For now this page is intentionally kept simple so the project has a
        working contact route without inventing an email address.
      </p>
    </SimplePage>
  );
}
